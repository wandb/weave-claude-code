{
  "name": "weave-claude-code",
  "version": "0.2.11",
  "description": "Track Claude Code sessions in Weave for observability and debugging",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "weave-claude-code": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && node scripts/build/copy-version-module.mjs",
    "build:watch": "tsc --watch",
    "dev": "tsx src/cli.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "check": "npm run build && npm test",
    "setup-hooks": "node scripts/install-git-hooks.mjs"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.1",
    "uuidv7": "1.2.1",
    "weave": "^0.16.2"
  },
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.178",
    "@opentelemetry/sdk-trace-base": "^2.7.1",
    "@types/node": "^18.19.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=18.19.0"
  },
  "files": [
    "dist/",
    "hooks/",
    "skills/",
    ".claude-plugin/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wandb/weave-claude-code.git"
  },
  "license": "MIT"
}
